# Tasks - Voice Recorder Layout & NovaGuy Animation Refinement

- [x] Modify `AudioVisualizer` Row height in `NovaBarUi.kt` to prevent timer shifts
- [x] Modify `NovaGuyCompactView` Canvas size in `NovaBarUi.kt` to scale dynamically
- [x] Modify `startEyeAnimation` logic in `OverlayStateManager.kt` to increase amplitude and holds
- [x] Verify build compiles and run debug/release checks
